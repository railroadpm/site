# MIDDLEWARE

This directory contains the app's middleware. Middleware allows custom logic to be executed
before rendering a page or a group of pages (layouts).

Details are available in the [middleware documentation](https://nuxtjs.org/guide/routing#middleware).
